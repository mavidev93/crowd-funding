/** @format */

import Divider from "@mui/material/Divider";

function CustomDivider( orientation:"horizontal" ) {
  return (
    <div>
      <Divider orientation={or} />
    </div>
  );
}

export default CustomDivider;
