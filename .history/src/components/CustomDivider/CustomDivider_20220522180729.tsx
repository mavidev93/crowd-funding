/** @format */

import Divider from "@mui/material/Divider";

function CustomDivider( orientation:h ) {
  return (
    <div>
      <Divider orientation={orientation} />
    </div>
  );
}

export default CustomDivider;
